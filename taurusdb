#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ ! -f "$ROOT_DIR/dist/index.js" ]; then
  echo "dist/index.js not found. Run: npm install && npm run build" >&2
  exit 1
fi

exec node "$ROOT_DIR/dist/index.js" "$@"
