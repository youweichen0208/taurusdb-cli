import { VERSION, COMMIT, BUILT_AT } from "../constants/version.js";
import { c } from "../output/chalk.js";

// Re-export c for other interactive modules
export { c };

export function versionLabel(): string {
  let v = VERSION.trim() || "dev";
  if (v === "dev") v = "vdev";
  if (v !== "vdev" && !v.startsWith("v")) v = `v${v}`;
  if (COMMIT.trim()) v = `${v} (build ${COMMIT.trim().slice(0, 7)})`;
  if (BUILT_AT.trim()) v = `${v} ${BUILT_AT.trim()}`;
  return v;
}

export function printBanner(profile: string, region: string): void {
  const red = c().redBright.bold;
  const yellow = c().yellow;
  const white = c().white.bold;
  const dim = c().gray;
  const taurus = [
    "  ████████╗ █████╗ ██╗   ██╗██████╗ ██╗   ██╗███████╗",
    "  ╚══██╔══╝██╔══██╗██║   ██║██╔══██╗██║   ██║██╔════╝",
    "     ██║   ███████║██║   ██║██████╔╝██║   ██║███████╗",
    "     ██║   ██╔══██║██║   ██║██╔══██╗██║   ██║╚════██║",
    "     ██║   ██║  ██║╚██████╔╝██║  ██║╚██████╔╝███████║",
    "     ╚═╝   ╚═╝  ╚═╝ ╚═════╝ ╚═╝  ╚═╝ ╚═════╝ ╚══════╝",
  ];
  const db = [
    " ██████╗ ██████╗ ",
    " ██╔══██╗██╔══██╗",
    " ██║  ██║██████╔╝",
    " ██║  ██║██╔══██╗",
    " ██████╔╝██████╔╝",
    " ╚═════╝ ╚═════╝ ",
  ];
  console.log("");
  for (let i = 0; i < taurus.length; i++) {
    console.log(`${red(taurus[i])}  ${yellow(db[i])}`);
  }
  console.log("");
  console.log(
    `${white("  华为云数据库命令行工具 + 智能 Agent")}    ${dim(versionLabel())}`,
  );
  console.log(dim(`  Profile=${profile}  Region=${region || "-"}`));
  console.log("");
}